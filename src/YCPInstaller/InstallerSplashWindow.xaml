<Window x:Class="YCPInstaller.InstallerSplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Installer Loading" Height="300" Width="500"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" Topmost="True"
        Loaded="Window_Loaded">
    <Window.Resources>
        <Storyboard x:Key="IntroAnimation" Completed="IntroAnimation_Completed">
            <!-- Fade in background -->
            <DoubleAnimation Storyboard.TargetName="BgBorder"
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.5"/>
            <!-- Data Stream typing effect (using width clip trick) -->
            <DoubleAnimation Storyboard.TargetName="CodeMask"
                             Storyboard.TargetProperty="Width"
                             From="0" To="400" Duration="0:0:1.5">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="LoadingText"
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.5" BeginTime="0:0:0.5"/>
        </Storyboard>
        <Storyboard x:Key="OutroAnimation" Completed="OutroAnimation_Completed">
            <DoubleAnimation Storyboard.TargetName="MainGrid"
                             Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:0.4"/>
        </Storyboard>
    </Window.Resources>

    <Grid x:Name="MainGrid" Opacity="1">
        <Border x:Name="BgBorder" CornerRadius="8" Opacity="0" Background="#0A0A0A" BorderBrush="#333" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="#00FFCC" BlurRadius="40" ShadowDepth="0" Opacity="0.3"/>
            </Border.Effect>
        </Border>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid HorizontalAlignment="Left">
                <!-- Clip the text to reveal it horizontally -->
                <Border x:Name="CodeMask" ClipToBounds="True" Width="0" HorizontalAlignment="Left">
                    <TextBlock Text="INITIALIZING YCP SYSTEM..." Foreground="#00FFCC" FontSize="20" FontWeight="Bold" FontFamily="Consolas" Width="400"/>
                </Border>
            </Grid>
            <TextBlock x:Name="LoadingText" Text="Decrypting Installer Packages [===============> ] 98%" Foreground="#777" FontSize="12" FontFamily="Consolas" Margin="0,10,0,0" Opacity="0"/>
        </StackPanel>
    </Grid>
</Window>

<Window x:Class="YCPLauncher.Views.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="YCP Launcher Loading" 
        Height="300" Width="500" 
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        Loaded="Window_Loaded">
    <Window.Resources>
        <Storyboard x:Key="IntroAnimation">
            <!-- Fade in background -->
            <DoubleAnimation Storyboard.TargetName="BgBorder"
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.3"/>
            <!-- Data Stream typing effect (using width clip trick) -->
            <DoubleAnimation Storyboard.TargetName="CodeMask"
                             Storyboard.TargetProperty="Width"
                             From="0" To="400" Duration="0:0:0.5">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="LoadingText"
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.3" BeginTime="0:0:0.15"/>
        </Storyboard>
        
        <Storyboard x:Key="OutroAnimation" Completed="OutroAnimation_Completed">
            <DoubleAnimation Storyboard.TargetName="MainGrid"
                             Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:0.25">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>

    <Grid x:Name="MainGrid" Opacity="1" Background="Transparent">
        <Border x:Name="BgBorder" CornerRadius="8" Opacity="0"
                Background="{DynamicResource BgDeepBrush}"
                BorderBrush="{DynamicResource BorderBrush2}" BorderThickness="1"/>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid HorizontalAlignment="Left">
                <!-- Clip the text to reveal it horizontally -->
                <Border x:Name="CodeMask" ClipToBounds="True" Width="0" HorizontalAlignment="Left">
                    <TextBlock Text="INITIALIZING YCP LAUNCHER..." Foreground="{DynamicResource AccentBrush}" FontSize="20" FontWeight="Bold" FontFamily="Consolas" Width="400"/>
                </Border>
            </Grid>
            <TextBlock x:Name="LoadingText" Text="PREPARING CLIENT ENVIRONMENT" Foreground="{DynamicResource TextDimBrush}" FontSize="11" FontFamily="Consolas" Margin="0,10,0,0" Opacity="0"/>
        </StackPanel>
    </Grid>
</Window>

<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:fw="clr-namespace:SourceChord.FluentWPF;assembly=FluentWPF"
                    xmlns:helpers="clr-namespace:YCPLauncher.Helpers">
    <!-- Dark Theme Brushes -->
    <SolidColorBrush x:Key="AccentBrush"      Color="#FF6B00"/>
    
    <!-- Backgrounds -->
    <SolidColorBrush x:Key="BgDeepBrush"      Color="#070709"/>
    <SolidColorBrush x:Key="BgDarkBrush"      Color="#0B0F19"/>
    <SolidColorBrush x:Key="BgCardBrush"      Color="#121827"/>
    <SolidColorBrush x:Key="BgSidebarBrush"   Color="#A0070709"/>
    <SolidColorBrush x:Key="BgOverlayBrush"   Color="#A0000000"/>
    
    <!-- Borders -->
    <SolidColorBrush x:Key="BorderBrush1"     Color="#1E293B"/>
    <SolidColorBrush x:Key="BorderBrush2"     Color="#2D3A4F"/>
    <SolidColorBrush x:Key="BorderBrushHover" Color="#FF6B00"/>
    
    <!-- Texts -->
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="#F1F5F9"/>
    <SolidColorBrush x:Key="TextMutedBrush"   Color="#94A3B8"/>
    <SolidColorBrush x:Key="TextDimBrush"     Color="#475569"/>
    
    <!-- Specific Elements -->
    <SolidColorBrush x:Key="CardBgBrush"      Color="#C0121827"/>
    <SolidColorBrush x:Key="ItemBgBrush"      Color="#151C2C"/>
    <SolidColorBrush x:Key="ItemBgHoverBrush" Color="#1E293B"/>
    <SolidColorBrush x:Key="ItemBorderBrush"  Color="#2D3A4F"/>
    <SolidColorBrush x:Key="AccentBgBrush"    Color="#331A05"/>
    
    <!-- Status -->
    <SolidColorBrush x:Key="SuccessBrush"     Color="#22C55E"/>
    <SolidColorBrush x:Key="WarningBrush"     Color="#F59E0B"/>
    <SolidColorBrush x:Key="ErrorBrush"       Color="#EF4444"/>

    <!-- Gradient Backgrounds -->
    <RadialGradientBrush x:Key="GlowAccentBrush">
        <GradientStop Color="#1AFF6B00" Offset="0"/>
        <GradientStop Color="#00FF6B00" Offset="1"/>
    </RadialGradientBrush>
    <RadialGradientBrush x:Key="GlowBlueBrush">
        <GradientStop Color="#140055FF" Offset="0"/>
        <GradientStop Color="#000055FF" Offset="1"/>
    </RadialGradientBrush>

    <!-- Shadow Colors -->
    <Color x:Key="ShadowColor">#000000</Color>

    <!-- Win10 Tile Reveal Style for RevealBorder -->
    <Style x:Key="TileRevealStyle" TargetType="helpers:RevealBorder">
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="HoverColor" Value="#66FFFFFF"/>
        <Setter Property="HoverRadius" Value="350"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="#22FFFFFF"/>
        <Setter Property="Padding" Value="24"/>
        <Setter Property="Background" Value="{DynamicResource CardBgBrush}"/>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <EventTrigger RoutedEvent="MouseEnter">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.02" Duration="0:0:0.2">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.02" Duration="0:0:0.2">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="MouseLeave">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.0" Duration="0:0:0.3">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.0" Duration="0:0:0.3">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>

    <!-- Modern ScrollBar -->
    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Width" Value="8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid>
                        <Border Background="Transparent" Width="8"/>
                        <Track Name="PART_Track" IsDirectionReversed="true">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0"/>
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0"/>
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb>
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border CornerRadius="4" Background="#33FFFFFF" Margin="1" x:Name="ThumbBorder"/>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="ThumbBorder" Property="Background" Value="#66FFFFFF"/>
                                                </Trigger>
                                                <Trigger Property="IsDragging" Value="True">
                                                    <Setter TargetName="ThumbBorder" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>

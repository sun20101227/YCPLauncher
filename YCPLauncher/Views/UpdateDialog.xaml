<Window x:Class="YCPLauncher.Views.UpdateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="发现新版本" Height="400" Width="550"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="NoResize" Topmost="True"
        Closing="Window_Closing">
    <Border CornerRadius="12" Background="#111827" BorderBrush="{StaticResource AccentBrush}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="#FF5500" BlurRadius="30" ShadowDepth="0" Opacity="0.4"/>
        </Border.Effect>
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="发现新版本" Foreground="{StaticResource AccentBrush}" FontSize="24" FontWeight="Bold" Grid.Row="0"/>
            <TextBlock x:Name="TxtVersion" Text="即将升级到最新版" Foreground="{StaticResource TextMutedBrush}" FontSize="14" Margin="0,5,0,20" Grid.Row="1"/>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="0,0,0,20">
                <TextBlock x:Name="TxtReleaseNotes" TextWrapping="Wrap" Foreground="{StaticResource TextPrimaryBrush}" FontSize="14" LineHeight="22"/>
            </ScrollViewer>

            <Grid Grid.Row="3" Margin="0,0,0,20" x:Name="ProgressGrid" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock x:Name="TxtProgress" Text="正在下载 0%" Foreground="{StaticResource TextMutedBrush}" FontSize="12" HorizontalAlignment="Right" Margin="0,0,0,5"/>
                <ProgressBar x:Name="DownloadProgressBar" Grid.Row="1" Height="6" Minimum="0" Maximum="100" Value="0" Background="#374151" Foreground="{StaticResource AccentBrush}" BorderThickness="0"/>
            </Grid>

            <Button x:Name="BtnUpdate" Content="下载并更新" Grid.Row="4" Style="{StaticResource OrangeButton}" Height="40" Click="BtnUpdate_Click"/>
        </Grid>
    </Border>
</Window>

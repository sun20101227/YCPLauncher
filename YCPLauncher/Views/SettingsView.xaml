<UserControl x:Class="YCPLauncher.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:helpers="clr-namespace:YCPLauncher.Helpers"
             mc:Ignorable="d" 
             Background="Transparent"
             d:DesignHeight="800" d:DesignWidth="800">

    <UserControl.Resources>
        <!-- Animated Toggle Switch Style -->
        <Style x:Key="AnimatedToggleSwitch" TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Width="44" Height="24" Cursor="Hand">
                            <Border x:Name="bgOff" CornerRadius="12" Background="{DynamicResource BorderBrush2}"/>
                            <Border x:Name="bgOn" CornerRadius="12" Background="{DynamicResource AccentBrush}" Opacity="0"/>
                            <Ellipse x:Name="dot" Width="20" Height="20" Fill="White" HorizontalAlignment="Left" Margin="2,0,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ThicknessAnimation Storyboard.TargetName="dot" Storyboard.TargetProperty="Margin" To="22,0,0,0" Duration="0:0:0.2">
                                                <ThicknessAnimation.EasingFunction><CubicEase EasingMode="EaseInOut"/></ThicknessAnimation.EasingFunction>
                                            </ThicknessAnimation>
                                            <DoubleAnimation Storyboard.TargetName="bgOn" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ThicknessAnimation Storyboard.TargetName="dot" Storyboard.TargetProperty="Margin" To="2,0,0,0" Duration="0:0:0.2">
                                                <ThicknessAnimation.EasingFunction><CubicEase EasingMode="EaseInOut"/></ThicknessAnimation.EasingFunction>
                                            </ThicknessAnimation>
                                            <DoubleAnimation Storyboard.TargetName="bgOn" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="Header" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
                    <ThicknessAnimation Storyboard.TargetName="Header" Storyboard.TargetProperty="Margin" From="0,20,0,0" To="0,0,0,0" Duration="0:0:0.4"><ThicknessAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></ThicknessAnimation.EasingFunction></ThicknessAnimation>
                    
                    <DoubleAnimation Storyboard.TargetName="Card1" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4" BeginTime="0:0:0.1"/>
                    <ThicknessAnimation Storyboard.TargetName="Card1" Storyboard.TargetProperty="Margin" From="0,20,0,24" To="0,0,0,24" Duration="0:0:0.5" BeginTime="0:0:0.1"><ThicknessAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></ThicknessAnimation.EasingFunction></ThicknessAnimation>

                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="30,22,30,30">
            <!-- HEADER -->
            <StackPanel x:Name="Header" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,24" Opacity="0">
                <Rectangle Width="4" Height="22" Fill="{DynamicResource AccentBrush}"
                           RadiusX="2" RadiusY="2" Margin="0,0,12,0">
                    <Rectangle.Effect>
                        <DropShadowEffect Color="#FF6B00" BlurRadius="10" ShadowDepth="0"/>
                    </Rectangle.Effect>
                </Rectangle>
                <TextBlock Text="系统设置" FontSize="18" FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimaryBrush}" FontFamily="Segoe UI" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- CARD 1: Appearance -->
            <helpers:RevealBorder x:Name="Card1" Style="{StaticResource TileRevealStyle}" Background="{DynamicResource CardBgBrush}" HoverColor="#44FF6B00" HoverRadius="400" BorderThickness="1" CornerRadius="12" Padding="24" Margin="0,0,0,24" Opacity="0">
                <StackPanel>
                    <TextBlock Text="外观设置" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="14" FontWeight="Bold" Margin="0,0,0,16"/>
                    
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="深色模式 (Dark Mode)" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="13" FontFamily="Segoe UI"/>
                            <TextBlock Text="开启后界面将以深色为主，护眼且具电竞感" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" Margin="0,4,0,0"/>
                        </StackPanel>
                        
                        <CheckBox Grid.Column="1" IsChecked="{Binding IsDarkMode}" Style="{StaticResource AnimatedToggleSwitch}" VerticalAlignment="Center"/>
                    </Grid>



                    <Grid Margin="0,20,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="减轻动画效果" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="13" FontFamily="Segoe UI"/>
                            <TextBlock Text="关闭部分非必要的 UI 动态缩放和特效以节省性能" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" Margin="0,4,0,0"/>
                        </StackPanel>
                        
                        <CheckBox Grid.Column="1" IsChecked="{Binding ReduceAnimations}" Style="{StaticResource AnimatedToggleSwitch}" VerticalAlignment="Center"/>
                    </Grid>

                    <Rectangle Height="1" Fill="{DynamicResource BorderBrush2}" Margin="0,24,0,24"/>
                    <TextBlock Text="系统行为" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="14" FontWeight="Bold" Margin="0,0,0,16"/>
                    
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="关闭时最小化到托盘" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="13" FontFamily="Segoe UI"/>
                            <TextBlock Text="开启后点击关闭按钮不会直接退出程序" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" Margin="0,4,0,0"/>
                        </StackPanel>
                        
                        <CheckBox Grid.Column="1" IsChecked="{Binding MinimizeToTray}" Style="{StaticResource AnimatedToggleSwitch}" VerticalAlignment="Center"/>
                    </Grid>

                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="开机自动启动" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="13" FontFamily="Segoe UI"/>
                        </StackPanel>
                        
                        <CheckBox Grid.Column="1" IsChecked="{Binding StartOnBoot}" Style="{StaticResource AnimatedToggleSwitch}" VerticalAlignment="Center"/>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="硬件加速 (GPU渲染)" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="13" FontFamily="Segoe UI"/>
                            <TextBlock Text="开启后将显著提升粒子动画和特效帧率" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" Margin="0,4,0,0"/>
                        </StackPanel>
                        
                        <CheckBox Grid.Column="1" IsChecked="True" Style="{StaticResource AnimatedToggleSwitch}" VerticalAlignment="Center"/>
                    </Grid>

                    <Rectangle Height="1" Fill="{DynamicResource BorderBrush2}" Margin="0,24,0,24"/>
                    <TextBlock Text="游戏启动参数" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="14" FontWeight="Bold" Margin="0,0,0,16"/>
                    
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="跳过启动动画 (-novid)" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="13" FontFamily="Segoe UI"/>
                            <TextBlock Text="加速游戏加载速度" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" Margin="0,4,0,0"/>
                        </StackPanel>
                        <CheckBox Grid.Column="1" IsChecked="True" Style="{StaticResource AnimatedToggleSwitch}" VerticalAlignment="Center"/>
                    </Grid>

                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="高频刷新率 (-freq 240)" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="13" FontFamily="Segoe UI"/>
                            <TextBlock Text="强制游戏以 240Hz 运行" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" Margin="0,4,0,0"/>
                        </StackPanel>
                        <CheckBox Grid.Column="1" IsChecked="True" Style="{StaticResource AnimatedToggleSwitch}" VerticalAlignment="Center"/>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="控制台 (-console)" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="13" FontFamily="Segoe UI"/>
                            <TextBlock Text="在游戏启动时自动打开控制台" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" Margin="0,4,0,0"/>
                        </StackPanel>
                        <CheckBox Grid.Column="1" IsChecked="False" Style="{StaticResource AnimatedToggleSwitch}" VerticalAlignment="Center"/>
                    </Grid>

                    <Rectangle Height="1" Fill="{DynamicResource BorderBrush2}" Margin="0,24,0,24"/>
                    <TextBlock Text="高级性能" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="14" FontWeight="Bold" Margin="0,0,0,16"/>
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="硬件加速渲染 (Hardware Acceleration)" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="13" FontFamily="Segoe UI"/>
                            <TextBlock Text="利用 GPU 加速 UI 动画，开启极致丝滑体验" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" Margin="0,4,0,0"/>
                        </StackPanel>
                        <CheckBox Grid.Column="1" IsChecked="{Binding HardwareAcceleration}" Style="{StaticResource AnimatedToggleSwitch}" VerticalAlignment="Center"/>
                    </Grid>
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="CS2 启动参数预加载" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="13" FontFamily="Segoe UI"/>
                            <TextBlock Text="在后台提前挂载启动参数，大幅提升进服速度" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" Margin="0,4,0,0"/>
                        </StackPanel>
                        <CheckBox Grid.Column="1" IsChecked="{Binding NetworkOptimization}" Style="{StaticResource AnimatedToggleSwitch}" VerticalAlignment="Center"/>
                    </Grid>

                    <Rectangle Height="1" Fill="{DynamicResource BorderBrush2}" Margin="0,24,0,24"/>
                    <TextBlock Text="存储与缓存" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="14" FontWeight="Bold" Margin="0,0,0,16"/>
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="清理本地缓存" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="13" FontFamily="Segoe UI"/>
                            <TextBlock Text="释放图片和临时文件占用的磁盘空间" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" Margin="0,4,0,0"/>
                        </StackPanel>
                        <Button Grid.Column="1" Command="{Binding ClearCacheCommand}" Content="一键清理" Style="{StaticResource GhostButton}" Height="30" Padding="15,0"/>
                    </Grid>
                    
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="卸载 YCP 启动器" Foreground="#EF4444" FontSize="13" FontFamily="Segoe UI" FontWeight="SemiBold"/>
                            <TextBlock Text="删除所有配置和文件，从本机彻底移除此程序" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" Margin="0,4,0,0"/>
                        </StackPanel>
                        <Button Grid.Column="1" Command="{Binding UninstallLauncherCommand}" Content="卸载" Style="{StaticResource GhostButton}" Foreground="#EF4444" BorderBrush="#EF4444" Height="30" Padding="15,0"/>
                    </Grid>
                </StackPanel>
            </helpers:RevealBorder>
        </StackPanel>
    </ScrollViewer>
</UserControl>

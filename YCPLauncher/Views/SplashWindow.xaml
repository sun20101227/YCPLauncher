<Window x:Class="YCPLauncher.Views.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="YCP Launcher Loading" 
        Height="400" Width="600" 
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        Loaded="Window_Loaded">
    <Window.Resources>
        <Storyboard x:Key="IntroAnimation">
            <!-- Fade In -->
            <DoubleAnimation Storyboard.TargetName="MainGrid"
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.6">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>

            <!-- Slide Up gently -->
            <DoubleAnimation Storyboard.TargetName="ContentTransform"
                             Storyboard.TargetProperty="Y"
                             From="20" To="0" Duration="0:0:0.8">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            
            <!-- Infinite Loading Spinner -->
            <DoubleAnimation Storyboard.TargetName="SpinnerTransform"
                             Storyboard.TargetProperty="Angle"
                             From="0" To="360" Duration="0:0:1.5" RepeatBehavior="Forever"/>
        </Storyboard>
        
        <Storyboard x:Key="OutroAnimation" Completed="OutroAnimation_Completed">
            <DoubleAnimation Storyboard.TargetName="MainGrid"
                             Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="ContentTransform"
                             Storyboard.TargetProperty="Y"
                             From="0" To="-10" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>

    <Grid x:Name="MainGrid" Opacity="0" Background="Transparent">
        <!-- Glassmorphism Background -->
        <Border CornerRadius="16" Background="#A0060A10" BorderBrush="#22FFFFFF" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="#000" BlurRadius="40" ShadowDepth="10" Opacity="0.5"/>
            </Border.Effect>
        </Border>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5">
            <StackPanel.RenderTransform>
                <TranslateTransform x:Name="ContentTransform" Y="20"/>
            </StackPanel.RenderTransform>

            <!-- Logo -->
            <Border Width="80" Height="80" CornerRadius="40" Margin="0,0,0,24" HorizontalAlignment="Center">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FF6B00" Offset="0"/>
                        <GradientStop Color="#FFAA00" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Border.Effect>
                    <DropShadowEffect Color="#FF6B00" BlurRadius="24" ShadowDepth="4" Opacity="0.4"/>
                </Border.Effect>
                <TextBlock Text="Y" Foreground="White" FontSize="36" FontWeight="Black" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2,0,0,0"/>
            </Border>

            <!-- App Name -->
            <TextBlock Text="YCP Launcher" Foreground="White" FontSize="24" FontWeight="Bold" FontFamily="Microsoft YaHei UI, Segoe UI" HorizontalAlignment="Center" Margin="0,0,0,8"/>
            <TextBlock Text="正在连接服务器并验证环境..." Foreground="#A0AAB5" FontSize="13" FontFamily="Microsoft YaHei UI, Segoe UI" HorizontalAlignment="Center" Margin="0,0,0,32"/>

            <!-- Modern Spinner -->
            <Path Stroke="{DynamicResource AccentBrush}" StrokeThickness="3" StrokeLineJoin="Round" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Width="30" Height="30" Stretch="Uniform" HorizontalAlignment="Center" RenderTransformOrigin="0.5,0.5"
                  Data="M 15,0 A 15,15 0 1 1 0,15">
                <Path.RenderTransform>
                    <RotateTransform x:Name="SpinnerTransform" Angle="0"/>
                </Path.RenderTransform>
            </Path>
        </StackPanel>
    </Grid>
</Window>

<UserControl x:Class="YCPLauncher.Views.LiveStreamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             xmlns:vlc="clr-namespace:LibVLCSharp.WPF;assembly=LibVLCSharp.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1080"
             Unloaded="UserControl_Unloaded">
    <Grid Background="Transparent">
        <Grid.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>

        <Grid Margin="24">
            <Grid.ColumnDefinitions>
                <!-- Video Section -->
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="16"/>
                <!-- Chat Section -->
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Video Player (LibVLCSharp) -->
            <Border Grid.Column="0" CornerRadius="12" Background="Black" BorderBrush="{DynamicResource BorderBrush2}" BorderThickness="1" ClipToBounds="True">
                <Grid>
                    <vlc:VideoView x:Name="VideoPlayer" />
                    <!-- Loading Overlay for Video -->
                    <Border x:Name="LoadingOverlay" Background="Black" Opacity="0.8">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="正在连接 RTMP 视频流..." Foreground="White" FontSize="16" Margin="0,0,0,16"/>
                            <ProgressBar IsIndeterminate="True" Width="200" Height="4" Foreground="{DynamicResource AccentBrush}" Background="#33FFFFFF" BorderThickness="0"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- Chat (WebView2) -->
            <Border Grid.Column="2" CornerRadius="12" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource BorderBrush2}" BorderThickness="1" ClipToBounds="True">
                <wv2:WebView2 x:Name="ChatWebView" DefaultBackgroundColor="Transparent"/>
            </Border>
        </Grid>
    </Grid>
</UserControl>

<Window x:Class="YCPLauncher.Views.TerminalLaunchDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TerminalLaunchDialog" Height="380" Width="640"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ShowInTaskbar="False"
        Topmost="True">
    
    <Border Background="#0B0F19" CornerRadius="8" BorderBrush="#1E293B" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.5" Color="Black"/>
        </Border.Effect>
        <Grid Margin="1">
            <!-- Header bar -->
            <Border Height="32" VerticalAlignment="Top" Background="#121827" CornerRadius="8,8,0,0">
                <Grid>
                    <TextBlock Text="[ YCP CS2 CONNECT TERMINAL ]" Foreground="#475569" FontFamily="Consolas" 
                               FontSize="12" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,8,0">
                        <Ellipse Width="12" Height="12" Fill="#1E293B" Margin="4,0"/>
                        <Ellipse Width="12" Height="12" Fill="#1E293B" Margin="4,0"/>
                        <Ellipse Width="12" Height="12" Fill="#1E293B" Margin="4,0"/>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Terminal Output -->
            <ScrollViewer x:Name="LogScroll" Margin="16,48,16,16" VerticalScrollBarVisibility="Hidden">
                <TextBlock x:Name="LogText" FontFamily="Consolas" FontSize="14" Foreground="#22C55E" 
                           TextWrapping="Wrap" LineHeight="22"/>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
